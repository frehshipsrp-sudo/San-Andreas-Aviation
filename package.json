{
  "name": "san-andreas-aviation-website",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --watch",
    "build": "eleventy",
    "clean": "powershell -NoProfile -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\""
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "markdown-it": "^14.1.1",
    "markdown-it-anchor": "^9.2.0"
  }
}
